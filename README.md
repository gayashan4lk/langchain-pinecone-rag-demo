# Langchain Pinecone Tutorials

uv init
Initialized project `langchain-pinecone-rag-demo`

`uv add jupyterlab ipykernel`

`python -m ipykernel install --user --name "my-uv-env" --display-name "Python (uv)"`

`jupyter lab`


## Run the notebook

`uv sync`

`source .venv/bin/activate`

`jupyter lab`
